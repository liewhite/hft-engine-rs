# Rust
/target/

# 回测历史数据缓存 (按需下载, 不入库)
/data-cache/

# 滚动的审查清单：本地草稿，不入库（历史上被误提交过两次，故显式忽略）
/review.md

# 回测产物 (PnL 时序 / 成交记录)
/gamma_pnl.json
/backtest-fills-*.csv

# IDE
.idea/
.vscode/
*.swp
*.swo

# Environment
.env
config.toml
config.json

# Test credentials
tests/ibkr_config.json

# OS
.DS_Store
*.pem
ibkr_config.json
hype_config.json
spread_arb_config.json

# 误入库的一次性文件（已从索引移除，本地保留）：
# AWSCLIV2.pkg 是 52MB 的 AWS CLI 安装包，与本项目无关；
# 三个 python 脚本是查资费的一次性草稿，功能早已由 FundingFee 事件通道覆盖。
# 注意：blob 仍在 git 历史里，真要瘦身得重写历史，本次不做。
/AWSCLIV2.pkg
/check_funding.py
/funding_test.py
/get_funding.py
